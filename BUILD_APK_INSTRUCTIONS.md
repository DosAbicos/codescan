# Инструкция по сборке APK для Android

## 📱 Что было исправлено

### Проблема
Таблица не загружалась в собранном APK, хотя в preview все работало нормально.

### Решение
1. ✅ Добавлена кнопка "Загрузить Excel файл" на главный экран
2. ✅ Добавлена кнопка "Загрузить новый файл" для замены таблицы
3. ✅ Добавлены необходимые разрешения в app.json
4. ✅ Улучшена обработка ошибок и логирование

---

## 🔧 Подготовка к сборке APK

### 1. Проверьте app.json
Убедитесь что файл `/app/frontend/app.json` содержит правильные разрешения:

```json
{
  "expo": {
    "android": {
      "permissions": [
        "READ_EXTERNAL_STORAGE",
        "WRITE_EXTERNAL_STORAGE",
        "READ_MEDIA_IMAGES",
        "READ_MEDIA_VIDEO",
        "READ_MEDIA_AUDIO"
      ],
      "package": "com.frontend.app"
    }
  }
}
```

### 2. Настройте backend URL для production

**Важно!** В production APK нужно указать реальный URL вашего backend сервера.

Отредактируйте файл `/app/frontend/.env`:

```bash
# Для production используйте реальный домен
EXPO_PUBLIC_BACKEND_URL=https://ваш-домен.com

# Или IP адрес вашего сервера
EXPO_PUBLIC_BACKEND_URL=http://192.168.1.100:8001
```

⚠️ **Внимание:** 
- Не используйте `localhost` или `127.0.0.1` в production APK
- Убедитесь что ваш backend сервер доступен из интернета или локальной сети
- Если используете HTTP (не HTTPS), добавьте домен в `android.usesCleartextTraffic`

---

## 🚀 Сборка APK с помощью EAS Build

### Шаг 1: Установите EAS CLI
```bash
npm install -g eas-cli
```

### Шаг 2: Войдите в аккаунт Expo
```bash
eas login
```

### Шаг 3: Настройте проект для EAS Build
```bash
cd /app/frontend
eas build:configure
```

### Шаг 4: Соберите APK для Android
```bash
# Development build (для тестирования)
eas build --platform android --profile development

# Production build (для распространения)
eas build --platform android --profile production
```

### Шаг 5: Скачайте готовый APK
После завершения сборки EAS предоставит ссылку для скачивания APK.

---

## 🏗️ Альтернатива: Локальная сборка APK

Если вы хотите собрать APK локально без EAS:

### 1. Установите Android Studio и SDK
- Скачайте Android Studio: https://developer.android.com/studio
- Установите Android SDK (API level 33+)
- Настройте переменные окружения `ANDROID_HOME`

### 2. Создайте standalone build
```bash
cd /app/frontend
npx expo prebuild
```

### 3. Соберите APK с помощью Gradle
```bash
cd android
./gradlew assembleRelease
```

APK будет находиться в:
```
android/app/build/outputs/apk/release/app-release.apk
```

---

## 🧪 Тестирование APK

### 1. Установите APK на устройство
```bash
adb install app-release.apk
```

### 2. Проверьте логи
```bash
adb logcat | grep -i "react"
```

### 3. Проверьте что работает:
- ✅ Кнопка "Загрузить Excel файл" видна на главном экране
- ✅ Файл успешно загружается и парсится
- ✅ Таблица отображается в списке товаров
- ✅ Сканирование штрихкодов работает
- ✅ Присвоение штрихкодов сохраняется

---

## 🐛 Решение проблем

### Проблема: Таблица не загружается
**Причины:**
1. Backend недоступен (проверьте URL в .env)
2. Нет разрешений на чтение файлов
3. Неправильный формат Excel файла

**Решение:**
1. Проверьте логи в adb logcat
2. Убедитесь что EXPO_PUBLIC_BACKEND_URL указывает на рабочий сервер
3. Проверьте что файл имеет расширение .xls (не .xlsx)

### Проблема: Ошибка "Network request failed"
**Причины:**
1. Backend сервер недоступен
2. Firewall блокирует соединение
3. Неправильный URL

**Решение:**
1. Проверьте что backend работает: `curl http://ваш-домен.com/api/session`
2. Убедитесь что порты открыты
3. Проверьте URL в .env файле

### Проблема: Ошибка при выборе файла
**Причины:**
1. Нет разрешений на доступ к файлам
2. Файл находится в защищенной директории

**Решение:**
1. Проверьте что разрешения добавлены в app.json
2. Используйте файлы из Downloads или Documents
3. Пересоберите APK после добавления разрешений

---

## 📞 Поддержка

Если у вас остались вопросы или проблемы:
1. Проверьте логи: `adb logcat`
2. Убедитесь что backend работает
3. Проверьте настройки .env файла
4. Пересоберите APK с правильными настройками

---

## ✅ Чек-лист перед сборкой

- [ ] Добавлены разрешения в app.json
- [ ] Настроен правильный EXPO_PUBLIC_BACKEND_URL в .env
- [ ] Backend сервер доступен и работает
- [ ] Проверено что sample_file.xls загружается в preview
- [ ] Установлен EAS CLI или Android SDK
- [ ] Готов аккаунт Expo (для EAS Build)

---

Удачи со сборкой! 🚀
