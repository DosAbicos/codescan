# 📱 Локальная сборка APK для Android

## Требования

- Windows/Mac/Linux
- Node.js 18+
- Android Studio
- JDK 17+

---

## Шаг 1: Установите Android Studio

1. Скачайте: https://developer.android.com/studio
2. Установите Android Studio
3. Откройте Android Studio → SDK Manager
4. Установите:
   - Android SDK Platform 34
   - Android SDK Build-Tools 34
   - Android SDK Command-line Tools

---

## Шаг 2: Настройте переменные окружения

### Windows:
```cmd
setx ANDROID_HOME "%LOCALAPPDATA%\Android\Sdk"
setx PATH "%PATH%;%LOCALAPPDATA%\Android\Sdk\platform-tools"
```

### Mac/Linux:
```bash
# Добавьте в ~/.bashrc или ~/.zshrc:
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/tools
```

---

## Шаг 3: Установите зависимости

```bash
cd /app/frontend
yarn install
```

---

## Шаг 4: Создайте native проект

```bash
npx expo prebuild --platform android --clean
```

Это создаст папку `android/` с native кодом.

---

## Шаг 5: Соберите APK

```bash
cd android
./gradlew assembleRelease
```

**Windows:** Используйте `gradlew.bat` вместо `./gradlew`

---

## Шаг 6: Найдите APK

APK находится в:
```
android/app/build/outputs/apk/release/app-release.apk
```

Размер: ~40-80 МБ

---

## Шаг 7: Установите APK на телефон

### Вариант A: Через USB

1. Включите "Отладка по USB" на телефоне:
   - Настройки → О телефоне
   - Нажмите 7 раз на "Номер сборки"
   - Настройки → Для разработчиков → Отладка по USB

2. Подключите телефон к компьютеру

3. Установите APK:
```bash
adb install android/app/build/outputs/apk/release/app-release.apk
```

### Вариант B: Скопируйте файл

1. Скопируйте `app-release.apk` на телефон (USB/Bluetooth/облако)
2. Откройте файл на телефоне
3. Разрешите "Установку из неизвестных источников"
4. Нажмите "Установить"

---

## ✅ Готово!

Приложение установлено и готово к работе!

При первом запуске:
- Автоматически загрузится таблица (3223 товара)
- Нажмите "Продолжить работу"
- Начните сканировать штрихкоды

---

## 🐛 Проблемы?

### "SDK location not found"
```bash
# Создайте файл android/local.properties:
sdk.dir=/path/to/Android/sdk
```

### "Gradle build failed"
```bash
cd android
./gradlew clean
./gradlew assembleRelease
```

###  "adb: command not found"
Добавьте Android SDK platform-tools в PATH.

### Приложение вылетает
Проверьте что backend работает и доступен.

---

## 📝 Примечания

- APK собирается **БЕЗ ПОДПИСИ** (для личного использования)
- Для публикации в Google Play нужна подпись
- Backend должен работать на локальной сети или интернете
- В `.env` укажите IP вашего компьютера:
  ```
  EXPO_PUBLIC_BACKEND_URL=http://192.168.1.XXX:8001
  ```
