# 📱 Инструкция по установке APK на Android

## 📋 Содержание
1. [Сборка APK](#сборка-apk)
2. [Установка APK на устройство](#установка-apk-на-устройство)
3. [Решение проблем](#решение-проблем)

---

## 🔨 Сборка APK

### Вариант 1: Через EAS Build (Рекомендуется)

#### Шаг 1: Установите EAS CLI
```bash
npm install -g eas-cli
```

#### Шаг 2: Войдите в аккаунт Expo
```bash
eas login
```
Введите email и пароль от вашего аккаунта Expo.

Если у вас нет аккаунта:
```bash
eas register
```

#### Шаг 3: Настройте проект
```bash
cd /app/frontend
eas build:configure
```

Выберите:
- Platform: **Android**
- Build profile: **production**

Это создаст файл `eas.json` с настройками сборки.

#### Шаг 4: ВАЖНО - Настройте Backend URL

Откройте `/app/frontend/.env` и укажите URL вашего backend сервера:

```bash
# Замените на ваш реальный домен
EXPO_PUBLIC_BACKEND_URL=https://ваш-домен.com

# Или IP адрес сервера
EXPO_PUBLIC_BACKEND_URL=http://192.168.1.100:8001
```

⚠️ **Важно:** Без правильного URL таблица не загрузится в APK!

#### Шаг 5: Запустите сборку APK

Для **development build** (для тестирования):
```bash
eas build --platform android --profile development
```

Для **production build** (для публикации):
```bash
eas build --platform android --profile production
```

#### Шаг 6: Дождитесь окончания сборки

Процесс сборки занимает 10-20 минут. Вы увидите:
```
✔ Build complete!
```

#### Шаг 7: Скачайте APK

После завершения EAS предоставит **ссылку для скачивания APK**.

Пример:
```
https://expo.dev/artifacts/eas/abcd1234.apk
```

Скачайте APK по этой ссылке.

---

### Вариант 2: Локальная сборка (Для опытных пользователей)

#### Требования:
- Android Studio
- Android SDK (API level 33+)
- Java JDK 17+

#### Шаги:

1. **Установите зависимости**
```bash
cd /app/frontend
npm install
```

2. **Создайте native проект**
```bash
npx expo prebuild --platform android
```

3. **Соберите APK**
```bash
cd android
./gradlew assembleRelease
```

4. **Найдите APK**
```bash
android/app/build/outputs/apk/release/app-release.apk
```

---

## 📲 Установка APK на устройство

### Способ 1: Через USB кабель (ADB)

#### Требования:
- USB кабель
- Android устройство
- ADB (входит в Android SDK)

#### Шаги:

**1. Включите режим разработчика на телефоне:**
- Откройте **Настройки** → **О телефоне**
- Нажмите 7 раз на **Номер сборки**
- Вернитесь в Настройки → **Для разработчиков**
- Включите **Отладку по USB**

**2. Подключите телефон к компьютеру через USB**

**3. Разрешите отладку по USB на телефоне**
- Появится диалог "Разрешить отладку по USB?"
- Нажмите **OK**

**4. Установите APK через ADB:**
```bash
adb install путь/к/app-release.apk
```

Или если скачали через EAS:
```bash
adb install ~/Downloads/build-1234567890.apk
```

**5. Проверьте установку:**
```bash
adb shell pm list packages | grep com.frontend.app
```

---

### Способ 2: Через файловый менеджер (Прямая установка)

#### Шаги:

**1. Скопируйте APK на телефон**
- Через USB: скопируйте файл в папку Downloads
- Или через облако (Google Drive, Dropbox и т.д.)
- Или отправьте себе по email/Telegram

**2. Включите установку из неизвестных источников:**

**Android 8.0+:**
- Откройте **Настройки** → **Безопасность**
- Найдите **Установка из неизвестных источников**
- Выберите **Файловый менеджер** или **Chrome**
- Включите переключатель

**Android 7.0 и ниже:**
- Откройте **Настройки** → **Безопасность**
- Включите **Неизвестные источники**

**3. Найдите APK файл:**
- Откройте **Файловый менеджер** (Files, Мои файлы и т.д.)
- Перейдите в папку **Downloads**
- Найдите файл `app-release.apk` или `build-XXXXXXX.apk`

**4. Установите приложение:**
- Нажмите на APK файл
- Нажмите **Установить**
- Дождитесь завершения установки
- Нажмите **Открыть** или найдите приложение в меню

---

### Способ 3: Через ссылку (Если APK размещен онлайн)

#### Если APK загружен на сервер или EAS Build:

**1. Откройте ссылку на телефоне:**
- Откройте браузер Chrome на телефоне
- Введите ссылку на APK (например, из EAS Build)

**2. Скачайте APK:**
- Chrome скачает файл автоматически
- Или нажмите на кнопку скачивания

**3. Установите:**
- После скачивания нажмите **Открыть**
- Или найдите файл в Downloads и следуйте инструкции "Способ 2"

---

### Способ 4: Через QR код

**1. Создайте QR код со ссылкой на APK:**
- Используйте сервис https://www.qr-code-generator.com/
- Вставьте ссылку на APK
- Создайте QR код

**2. Отсканируйте QR код:**
- Откройте камеру на телефоне
- Наведите на QR код
- Нажмите на уведомление
- Скачайте и установите APK

---

## 🔍 Проверка установки

### После установки проверьте:

**1. Откройте приложение**
- Найдите иконку "Штрихкоды" на главном экране
- Нажмите на иконку

**2. Проверьте загрузку таблицы:**
- При первом запуске должна автоматически загрузиться таблица `sample_file.xls`
- Вы увидите: **"3223 товара"**
- Нажмите **"Продолжить работу"**

**3. Проверьте список товаров:**
- Должен открыться список товаров
- Вкладка "Без штрихкода (3223)"
- Есть поиск товаров
- Есть кнопка "Сканировать"

**4. Проверьте сканер:**
- Нажмите кнопку **"Сканировать"**
- Приложение запросит доступ к камере
- Нажмите **"Разрешить"**
- Должна открыться камера для сканирования

✅ Если все работает - установка прошла успешно!

---

## 🔧 Решение проблем

### Проблема 1: "Установка заблокирована"

**Причина:** Не включены "Неизвестные источники"

**Решение:**
1. Откройте **Настройки** → **Безопасность**
2. Включите **Установка из неизвестных источников**
3. Или разрешите для конкретного приложения (Chrome, Files)
4. Попробуйте установить снова

---

### Проблема 2: "Приложение не установлено"

**Причина 1:** Уже установлена старая версия

**Решение:**
1. Удалите старую версию приложения
2. Установите новую версию

**Причина 2:** Недостаточно места

**Решение:**
1. Проверьте свободное место (Настройки → Хранилище)
2. Освободите минимум 100 МБ
3. Попробуйте снова

**Причина 3:** Поврежденный APK файл

**Решение:**
1. Скачайте APK заново
2. Проверьте размер файла (должен быть 40-80 МБ)
3. Попробуйте установить снова

---

### Проблема 3: "Таблица не загружается в APK"

**Причина:** Неправильный URL backend или backend недоступен

**Решение:**

**1. Проверьте .env файл:**
```bash
# Должен быть реальный URL, не localhost
EXPO_PUBLIC_BACKEND_URL=https://ваш-домен.com
```

**2. Проверьте что backend работает:**
- Откройте браузер на телефоне
- Введите: `https://ваш-домен.com/api/session`
- Должен вернуться JSON: `{"session": ...}`

**3. Пересоберите APK с правильным URL:**
```bash
cd /app/frontend
# Исправьте .env
eas build --platform android --profile production
```

**4. Временное решение - включите мобильный хотспот:**
- Если backend на вашем компьютере
- Включите мобильный хотспот на телефоне
- Подключите компьютер к хотспоту
- Узнайте IP телефона (Настройки → О телефоне → Статус)
- Используйте этот IP в EXPO_PUBLIC_BACKEND_URL

---

### Проблема 4: "Parse Error: There was a problem parsing the package"

**Причина:** APK собран для другой версии Android

**Решение:**
1. Проверьте версию Android (Настройки → О телефоне)
2. Приложение требует Android 5.0+ (API 21+)
3. Если версия подходит - пересоберите APK
4. Или попробуйте установить на другое устройство

---

### Проблема 5: "Камера не работает при сканировании"

**Причина:** Не предоставлены разрешения

**Решение:**
1. Откройте **Настройки** → **Приложения**
2. Найдите **Штрихкоды**
3. Откройте **Разрешения**
4. Включите **Камера**
5. Включите **Хранилище** (для загрузки файлов)
6. Перезапустите приложение

---

### Проблема 6: "adb: device not found"

**Причина:** Телефон не распознан компьютером

**Решение:**

**1. Проверьте USB кабель:**
- Используйте оригинальный кабель
- Попробуйте другой USB порт

**2. Установите драйверы:**
- Windows: Google USB Driver
- Mac: не требуется
- Linux: добавьте udev rules

**3. Проверьте режим USB:**
- На телефоне откройте уведомления
- Нажмите "USB charging"
- Выберите **"File Transfer"** или **"MTP"**

**4. Проверьте список устройств:**
```bash
adb devices
```
Должно показать: `XXXXXXXX device`

---

## 📝 Полезные команды ADB

### Проверить подключенные устройства:
```bash
adb devices
```

### Установить APK:
```bash
adb install app-release.apk
```

### Переустановить APK (сохранив данные):
```bash
adb install -r app-release.apk
```

### Удалить приложение:
```bash
adb uninstall com.frontend.app
```

### Посмотреть логи приложения:
```bash
adb logcat | grep -i "штрихкоды\|barcode\|react"
```

### Очистить логи:
```bash
adb logcat -c
```

### Сделать скриншот:
```bash
adb shell screencap -p /sdcard/screenshot.png
adb pull /sdcard/screenshot.png
```

### Запустить приложение:
```bash
adb shell am start -n com.frontend.app/.MainActivity
```

---

## 🎯 Чек-лист перед установкой

### Backend:
- [ ] Backend сервер запущен и доступен
- [ ] URL backend указан в .env
- [ ] Можно открыть `https://ваш-домен.com/api/session` в браузере
- [ ] sample_file.xls находится в `/app/sample_file.xls`

### Android устройство:
- [ ] Android версия 5.0+ (API 21+)
- [ ] Включена "Отладка по USB" (для установки через ADB)
- [ ] Включена "Установка из неизвестных источников"
- [ ] Свободно минимум 100 МБ памяти
- [ ] Разрешения на камеру и хранилище (после установки)

### APK файл:
- [ ] APK собран с правильным EXPO_PUBLIC_BACKEND_URL
- [ ] Размер APK 40-80 МБ (нормальный размер)
- [ ] Файл не поврежден (можно открыть архиватором)
- [ ] Package name: com.frontend.app

---

## 📱 Поддерживаемые устройства

### Минимальные требования:
- **Android:** 5.0 (API 21) и выше
- **RAM:** 2 ГБ минимум, 4 ГБ рекомендуется
- **Камера:** Для сканирования штрихкодов
- **Память:** 100 МБ свободного места

### Проверено на:
- Samsung Galaxy (Android 10+)
- Xiaomi / Redmi (MIUI 12+)
- Huawei (Android 9+)
- OnePlus (OxygenOS 11+)
- Google Pixel (Android 11+)

---

## 🆘 Дополнительная помощь

### Если ничего не помогло:

1. **Проверьте логи:**
```bash
adb logcat | grep -E "(ERROR|FATAL)"
```

2. **Пересоберите APK с нуля:**
```bash
cd /app/frontend
rm -rf android/
npx expo prebuild --platform android --clean
cd android
./gradlew clean
./gradlew assembleRelease
```

3. **Попробуйте development build вместо production:**
```bash
eas build --platform android --profile development
```

4. **Проверьте EAS Build логи:**
- Зайдите на https://expo.dev/
- Откройте ваш проект
- Найдите build и посмотрите логи

---

## ✅ Успешная установка!

После успешной установки:

1. Откройте приложение "Штрихкоды"
2. При первом запуске автоматически загрузится таблица (3223 товара)
3. Нажмите "Продолжить работу"
4. Можете сканировать штрихкоды и присваивать их товарам
5. Экспортировать обновленную таблицу Excel

**Готово! Приложение работает!** 🎉

---

## 📚 Дополнительные материалы

- [Официальная документация Expo](https://docs.expo.dev/)
- [EAS Build Guide](https://docs.expo.dev/build/introduction/)
- [Android Studio](https://developer.android.com/studio)
- [ADB документация](https://developer.android.com/studio/command-line/adb)
